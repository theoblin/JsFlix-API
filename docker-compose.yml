version: "3.5"

services:
  postgres:
    image: postgres:11.6-alpine
    ports:
      - "5433:5432"
    environment:
      - POSTGRES_DB=jsflix
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=pass
